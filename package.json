{
  "name": "@nxus/core",
  "main": "lib/index.js",
  "version": "2.0.1",
  "description": "A framework for building light-weight, event-driven data processing apps.",
  "scripts": {
    "test": "npm run compile && NODE_ENV=test mocha --compilers js:babel/register -R spec test/*",
    "compile": "babel --optional runtime -d lib/ src/",
    "prepublish": "npm run compile",
    "docs": "./node_modules/documentation/bin/documentation.js ./src/index.js -f html -o ./docs"
  },
  "author": "info@nxusapp.com",
  "license": "MIT",
  "dependencies": {
    "async": "^0.9.0",
    "babel-runtime": "^5.7.0",
    "bluebird": "^3.0.5",
    "chokidar": "^1.0.1",
    "findup-sync": "^0.2.1",
    "merge-descriptors": "0.0.2",
    "moment": "^2.10.3",
    "multimatch": "^2.0.0",
    "underscore": "*"
  },
  "devDependencies": {
    "babel": "^5.6.23",
    "documentation": "^3.0.4",
    "mocha": "^2.2.5",
    "should": "^7.0.2"
  }
}
